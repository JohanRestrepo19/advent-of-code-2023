import { DayProblem } from '@/lib/types'

export const partOne: DayProblem = async inputFileName => {
    console.log({ inputFileName })
    return -1
}

//--------------------------------------------------------------------

export const parTwo: DayProblem = async inputFileName => {
    console.log({ inputFileName })
    return -1
}
